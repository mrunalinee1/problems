What will be the output of this code, if the user enters 4?

process.stdin.setEncoding('utf8');

process.stdin.on('data', function (input) {
   var num = parseInt(input); 
   console.log("The number is:", num);
});

Note: The parseInt() is an inbuilt function in JavaScript that is used to parse a string and convert it into an integer (whole number).
The default data type of input in JS is String and hence we need to convert it to integer if we need an integer.
